# Miniplan: project basics, 2026-08-18

Seven tasks, #93 to #99. Two are deliberately reordered against the numbering in the request;
the reasons are given below, because both reorderings are about not repeating a failure this
project has already had.

---

## The finding that changes the shape of this

**The loss is bigger than `PENDING_FOLLOWUPS.md` recorded.** That note says two annotation
directories under `biotype_probe/expression_context_human/data/` were deleted. In fact the
**entire `experiments/biotype_probe/` tree is gone** -- all of it, including `methods.md`,
`results.md`, `scripts/`, `figures/` and every sub-experiment (`short_orf_subset`,
`embedding_geometry`, `expression_context`).

Evidence:

- `RNAZoo/experiments/` now contains only `orthrus_probe` and `riboseq_signal_model`.
- `RNAZoo/.gitignore` line 52 is `/experiments/`, so **it was never in git.** The riboseq repo has
  no history of it either.
- No tarball or archive copy found under `RNAZoo_meta/` or `carpenterlab/emalekos/`.
- `experiments/` directory mtime is **2026-08-15 13:38:59**, which pins when the entry was removed.

This is why task #93 (why is it missing) is scheduled FIRST, ahead of the RiboCode rerun.

**The good news: the rerun is feasible.** The inputs all survive.

| needed | status |
|---|---|
| per-tissue Ribo BAMs | `data/ribo_bam_e2e_human/` -- 8 training tissues + Hepatocytes, `.Aligned.toTranscriptome.out.bam` |
| Brain + the rest | `data/chothani_bam/` -- 445 entries |
| RiboCode annotation | `data/human_ribocode_annot/` -- all three files |

No surviving `*_collapsed.txt` for any human tissue anywhere, so regeneration is the only route.

---

## Order, and why it differs from the request

| run | task | request # | why here |
|--:|---|---|---|
| 1 | #93 why is the data missing | 2 | **Moved earlier.** Gates #97. Regenerating into an unexplained deletion mechanism risks losing it a second time. Also tells us whether anything else is exposed. Cheap. |
| 2 | #94 pack builder CLI | 0.1 | Self-contained, no dependencies, first in the request. |
| 3 | #95 reclaim A549 + iPSC FASTQs | 3 | **Moved earlier.** The group ceph is at **13.45 of 15 TB (89.6%)**. RiboCode across 9 tissues writes real output, and EDQUOT on this filesystem kills SLURM jobs silently with exit 120 and no traceback -- it has already killed a run on this project. Free space BEFORE the big job, not after. |
| 4 | #96 repoint the 4 sbatch | 1.1 | Cheap. Puts the annotation path right before #97 depends on it. |
| 5 | #97 rerun RiboCode | 1.2 | The one heavy compute item. Blocked by #93, #95, #96. |
| 6 | #98 figure provenance + standing rule | 4 | Runs while #97 sits in the SLURM queue. No compute. |
| 7 | #99 citations into methods.md | 5 | Same -- fills queue time. |

Only two things moved, and both moved for the same reason: **do not start a long SLURM job until
the filesystem has room and the cause of the last data loss is known.**

---

## Scope notes per task

**#94 pack builder.** `prepare_pack.py`, `prepare_from_bams.py` and `prepare_packs.py` all exist
already. The unfinished piece is plan phase 2 item 6: five duplicate scripts
(`pool_rnaseq_coverage{,_tissue}.py`, `pack_target_coverage{,_tissue}.py`, `build_psite_target.py`)
still carry hardcoded roots and `len(files)==32` style asserts, and **not one of them imports
`packlib`**. Validation gate is the one already written into the plan: rebuild a single tissue pack
and assert `target_counts.npy` is byte-identical to the existing one.

**#95 reclaim.** Two targets, about 150 GB, roughly 1% of quota -- small, as you said, but it is the
right 1% to free before #97.

| path | size | why safe |
|---|--:|---|
| `proteogenomics/data/A549_pilot` | 107.8 GB | Already deliberately archived 2026-08-15; `_archive_a549_2026_08_15/` preserves `call_summary.json`, `db_summary.json`, `assay/fragger.params` and the README |
| `proteogenomics/data/pgx_a549_shared` | 8.7 GB | same decision |
| `data/external/ruizorera2024/fastq` | 33 GB | BAMs survive at `data/heldout_bam/human_ruizorera`; the pack is built |

**I will run a reference check before any `rm`.** The A549 archive README explicitly says A549 still
anchors figures D12, S_rescore_substrate and S_fdr_rigor. S_rescore_substrate is retired, but D12 and
S_fdr_rigor are live. If either re-derives from `A549_pilot` at regeneration time rather than from a
committed values file, that path stays. The exact delete list comes back before it is executed.

**#97 output location.** The regenerated calls go somewhere inside `riboseq_signal_model`, NOT back
into the deleted `biotype_probe` tree. The 15 dependent files get repointed to the new location.
Choosing that path is part of the task.

**#98 standing rule.** Backfilling 16 files is the easy half. The half that matters is making it
enforceable: a checker that fails when a `*_values.json` has no `model`/`source`, wired into the
figure path, plus the rule written into `CLAUDE.md` next to the existing `FIGURE_DATA_INPUTS.md`
rule. A convention that is not checked is the reason 16 files drifted in the first place.

---

## Not in scope here

Electives (posture-B rebuild, CTG isolation, 3-seed V1 confirm, manuscript figures) stay parked.
The 198 uncommitted changes in the riboseq repo stay uncommitted -- no commits without instruction.
Task #83 (tutorial push) stays blocked on the same.

One loose end unrelated to all of this: SLURM job 36767377 `enc12a_merge` has been PENDING since
Aug 13 with `DependencyNeverSatisfied` and will never run. It wants a `scancel`.
